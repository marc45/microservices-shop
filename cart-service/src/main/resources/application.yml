server:
  port: 8080

spring:
  application:
    name: cart-service
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/carts
    username: shop
    password: 123
    driverClassName: org.postgresql.Driver